# ShopeePay SNAP Integration
Project ini adalah tool top-up ShopeePay menggunakan standar SNAP BI.

## Cara Pakai
1. Clone repo ini.
2. Jalankan `npm install`.
3. Buat file `.env` berdasarkan `.env.example`.
4. Masukkan `private_key.pem` kamu.
5. Jalankan `node server.js`.
